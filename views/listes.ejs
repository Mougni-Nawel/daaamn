<%- include("_header") -%>

<h1>Liste des Devoirs (<%= model.length %>)</h1>

<div class="table-responsive-sm">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Document</th>
        <th>Nom du devoir</th>
        <th>Proprietaire</th>
        <th>Commentaires</th>
        <th class="d-print-none">
          <a class="btn btn-sm btn-success" href="/create">Ajouter</a>
        </th>
      </tr>
    </thead>
    <tbody>
      <% for (const book of model) { %>
        <tr>
          <td><%= book.Photos %></td>
          <td><%= book.Nomplante %></td>
          <td><%= book.Proprietaire %></td>
          <td><%= book.Commentaires %></td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>

<%- include("_footer") -%>


<!--
<% include("_header") %>

<h1>Liste des livres</h1>

<ul>

  <% for (const book of model) { %>
    <li>
      <%= book.Titre %>
      <em>(<%= book.Auteur %>)</em>
    </li>
  <% } %>

</ul>

<% include("_footer") %>
-->
